apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-gateway
spec:
  rules:
  - http:
      paths:
      - path: /submit
        pathType: Prefix
        backend:
          service:
            name: submission-service
            port:
              number: 80