apiVersion: v1
kind: ConfigMap
metadata:
  name: pgbouncer-class-config
data:
  pgbouncer.ini: |
    [databases]
    classes = host=class-db port=5432 dbname=classes user=dev password=devpassword

    [pgbouncer]
    listen_addr = 0.0.0.0
    listen_port = 6432
    auth_type = md5
    auth_file = /etc/pgbouncer/userlist.txt
    pool_mode = transaction
    max_client_conn = 100
    default_pool_size = 20
  userlist.txt: |
    "dev" "devpassword"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-class
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-class
  template:
    metadata:
      labels:
        app: service-class
    spec:
      containers:
      - name: service-class
        image: service-class:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql+asyncpg://dev:devpassword@localhost:6432/classes"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "250m"
            memory: "128Mi"
      - name: pgbouncer
        image: edoburu/pgbouncer:latest
        volumeMounts:
        - name: pgbouncer-config
          mountPath: /etc/pgbouncer/pgbouncer.ini
          subPath: pgbouncer.ini
        - name: pgbouncer-config
          mountPath: /etc/pgbouncer/userlist.txt
          subPath: userlist.txt
        ports:
        - containerPort: 6432
      volumes:
      - name: pgbouncer-config
        configMap:
          name: pgbouncer-class-config
---
apiVersion: v1
kind: Service
metadata:
  name: service-class
spec:
  selector:
    app: service-class
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP