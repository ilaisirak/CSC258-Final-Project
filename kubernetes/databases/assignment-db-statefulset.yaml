apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: assignment-db
spec:
  serviceName: assignment-db   # must match the Service name
  replicas: 1                  # single primary; read replicas would be separate
  selector:
    matchLabels:
      app: assignment-db
  template:
    metadata:
      labels:
        app: assignment-db
    spec:
      containers:
      - name: postgres
        image: postgres:16
        ports:
        - containerPort: 5432
          name: postgres
        envFrom:
        - secretRef:
            name: assignment-db-secret
        # Optional: set additional PG settings
        env:
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "dev", "-d", "assignments"]
          initialDelaySeconds: 10
          periodSeconds: 5
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi   # adjust size