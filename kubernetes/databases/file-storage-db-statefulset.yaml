apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: file-storage-db
spec:
  serviceName: file-storage-db
  replicas: 1
  selector:
    matchLabels:
      app: file-storage-db
  template:
    metadata:
      labels:
        app: file-storage-db
    spec:
      containers:
      - name: postgres
        image: postgres:16
        ports:
        - containerPort: 5432
          name: postgres
        envFrom:
        - secretRef:
            name: file-storage-db-secret
        env:
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "dev", "-d", "file_storage"]
          initialDelaySeconds: 10
          periodSeconds: 5
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 5Gi
