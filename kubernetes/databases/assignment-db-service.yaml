apiVersion: v1
kind: Service
metadata:
  name: assignment-db
spec:
  selector:
    app: assignment-db
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  type: ClusterIP   # only reachable inside the cluster