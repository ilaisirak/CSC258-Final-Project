apiVersion: v1
kind: Service
metadata:
  name: submission-db
spec:
  selector:
    app: submission-db
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  type: ClusterIP   # only reachable inside the cluster